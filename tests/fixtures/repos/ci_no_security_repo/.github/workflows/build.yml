# Fixture: ci_no_security_repo
# CI workflow with no dependency or container scanning — GAP evidence for SI-2/RA-5.

name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install
        run: pip install -e .

      - name: Test
        run: pytest
